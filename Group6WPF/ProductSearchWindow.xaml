<Window x:Class="Group6WPF.ProductSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Group6WPF"
        mc:Ignorable="d"
        Title="ProductSearchWindow" Height="800" Width="1500"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Loaded="Window_Loaded">

	<DockPanel Width="1200" HorizontalAlignment="Center" Background="White">

		<!-- HEADER -->
		<Border DockPanel.Dock="Top" Height="70" Background="White" BorderBrush="#EBEBF0" BorderThickness="0,0,0,1">
			<Grid VerticalAlignment="Center">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<!-- Logo as a button -->
				<Button Grid.Column="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Back to Home"
                    Click="LogoButton_Click"
                    Margin="20,0,10,0"
                    Width="110"
                    Height="40">
					<Image Source="Assets/tiki.png" Stretch="Uniform"/>
				</Button>

				<!-- Search -->
				<Border Grid.Column="1" Height="40" Margin="20,0" VerticalAlignment="Center"
Background="#F8F8FF" BorderBrush="{StaticResource BorderColor}"
BorderThickness="1" CornerRadius="8">
					<TextBox x:Name="SearchTextBox" Height="38" Padding="15,0" FontSize="14" BorderThickness="0"
					 Background="Transparent" VerticalAlignment="Center"
					 Text="Search product by name..."
					 Foreground="{StaticResource TextSecondary}" KeyDown="SearchTextBox_KeyDown"/>
				</Border>

				<!-- Account Icon -->
				<Button x:Name="btnProfile" Grid.Column="2" Margin="10,0" Width="40" Height="40"
                    Background="Transparent" BorderThickness="0" ToolTip="Account"
                    Cursor="Hand" Click="btnProfile_Click">
					<Image Source="Assets/account.png" Stretch="Uniform"/>
				</Button>

				<!-- Cart Icon -->
				<Grid Grid.Column="3" Margin="10,0">
					<Button x:Name="btnViewCart" Width="40" Height="40"
                        Background="Transparent" BorderThickness="0" ToolTip="Shopping Cart"
                        Cursor="Hand" Click="btnViewCart_Click">
						<Image Source="Assets/cart.png" Stretch="Uniform"/>
					</Button>

					<!-- Cart Count Badge -->
					<Border x:Name="CartBadge"
                        Width="20" Height="20"
                        CornerRadius="10"
                        Background="#FF424E"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,-5,-5,0"
                        Visibility="Collapsed">
						<TextBlock x:Name="CartCountText"
                               Text="0"
                               Foreground="White"
                               FontSize="10"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
					</Border>
				</Grid>
			</Grid>
		</Border>

		<!-- MAIN CONTENT AREA -->
		<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
			<StackPanel Margin="30,20,30,30">
				<!-- Filter Controls Section -->
				<Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="500"/>
							<ColumnDefinition Width="500"/>
						</Grid.ColumnDefinitions>

						<!-- Sort Controls -->
						<StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
							<TextBlock Text="Sort by:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Medium" Foreground="#333"/>
							<ComboBox x:Name="SortComboBox" Width="150" Height="35"
                                      Background="White" BorderBrush="#DDD" BorderThickness="1"
                                      SelectionChanged="SortComboBox_SelectionChanged">
								<ComboBoxItem Content="Name" Tag="name"/>
								<ComboBoxItem Content="Most Expensive" Tag="expensive"/>
								<ComboBoxItem Content="Cheapest" Tag="cheap"/>
								<ComboBoxItem Content="Highest Rating" Tag="rating"/>
								<ComboBoxItem Content="Most Popular" Tag="trend" IsSelected="True"/>
							</ComboBox>
						</StackPanel>

						<!-- Price Range Filter -->
						<StackPanel Grid.Column="1" Margin="30,0,0,0">
							<Grid Margin="0,0,0,10">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>

								<TextBlock Grid.Column="0" Text="Price Range:" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#333"/>
								<TextBlock x:Name="PriceRangeDisplay" Grid.Column="1" Text="0đ - 10,000,000đ"
                                           VerticalAlignment="Center" FontWeight="Medium" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right"/>
							</Grid>

							<!-- Range Slider Container -->
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto"/>
									<RowDefinition Height="Auto"/>
									<RowDefinition Height="Auto"/>
								</Grid.RowDefinitions>

								<!-- Min Price Slider -->
								<Grid Grid.Row="0" Margin="0,0,0,5">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<TextBlock Grid.Column="0" Text="Min:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="12" Foreground="#666"/>
									<Slider x:Name="MinPriceSlider" Grid.Column="1"
                                            Minimum="0" Maximum="10000000" Value="0"
                                            TickFrequency="500000" IsSnapToTickEnabled="True"
                                            ValueChanged="PriceSlider_ValueChanged"
                                            Foreground="#FF424E" Background="#E0E0E0"/>
								</Grid>

								<!-- Max Price Slider -->
								<Grid Grid.Row="1" Margin="0,0,0,5">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<TextBlock Grid.Column="0" Text="Max:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="12" Foreground="#666"/>
									<Slider x:Name="MaxPriceSlider" Grid.Column="1"
                                            Minimum="0" Maximum="10000000" Value="10000000"
                                            TickFrequency="500000" IsSnapToTickEnabled="True"
                                            ValueChanged="PriceSlider_ValueChanged"
                                            Foreground="#FF424E" Background="#E0E0E0"/>
								</Grid>

								<!-- Price Labels -->
								<Grid Grid.Row="2" Margin="35,0,0,0">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="*"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>

									<TextBlock Grid.Column="0" Text="0đ" HorizontalAlignment="Left" FontSize="10" Foreground="#999"/>
									<TextBlock Grid.Column="1" Text="10,000,000đ" HorizontalAlignment="Right" FontSize="10" Foreground="#999"/>
								</Grid>
							</Grid>
						</StackPanel>
					</Grid>
				</Border>

				<!-- Product Cards Container - 4 items per row -->
				<UniformGrid x:Name="ProductGrid"
                             Columns="4"
                             HorizontalAlignment="Stretch"
                             Margin="0,10"/>

				<!-- Pagination Controls -->
				<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
					<Button x:Name="btnPrevious" Content="← Previous"
                            Width="100" Height="35" Margin="5,0"
                            BorderBrush="#DDD"
                            Cursor="Hand" Click="btnPrevious_Click"/>

					<TextBlock x:Name="txtPageInfo"
                               Text="Page 1 of 1"
                               VerticalAlignment="Center"
                               Margin="20,0" FontSize="14"/>

					<Button x:Name="btnNext" Content="Next →"
                            Width="100" Height="35" Margin="5,0"
                            BorderBrush="#DDD"
                            Cursor="Hand" Click="btnNext_Click"/>
				</StackPanel>

			</StackPanel>
		</ScrollViewer>
	</DockPanel>
</Window>