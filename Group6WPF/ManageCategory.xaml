<Window x:Class="Group6WPF.ManageCategory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Group6WPF"
        mc:Ignorable="d"
        Title="ManageCategory" Height="450" Width="800">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="txtSearch" Width="200" Margin="0,0,10,0"  Text="Search By Name..." />
            <Button Content="Search" Width="80" Margin="10,0,0,0" Click="BtnSearch_Click"/>
            <Button Content="Refresh" Width="80" Margin="10,0,0,0" Click="BtnRefresh_Click"/>
        </StackPanel>


        <DataGrid x:Name="dgCategories" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CategoryID}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Picture" Binding="{Binding Picture}" Width="*"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>


        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add" Width="80" Margin="0,0,10,0" Click="BtnAdd_Click"/>
            <Button Content="Edit" Width="80" Margin="0,0,10,0" Click="BtnEdit_Click"/>
            <Button Content="Delete" Width="80" Margin="0,0,10,0" Click="BtnDelete_Click"/>
        </StackPanel>

    </Grid>
</Window>
